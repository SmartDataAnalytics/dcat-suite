PREFIX dcat: <http://www.w3.org/ns/dcat#>
SELECT ?s
{
  ?s ?p ?o
  FILTER(?p = dcat:distribution || ?p = dcat:downloadURL)
  FILTER(REGEX(STR(?s), <env:ARG>, "i") || REGEX(STR(?o), <env:ARG>, "i"))
}
